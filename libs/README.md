# libs/

Vendored third-party libraries that are not managed by Conan.

Use this for:
- Header-only libraries you want to bundle directly
- Libraries not available in Conan's package index
- Forked or patched third-party code

For libraries available in Conan, add them to `conanfile.py` instead.
